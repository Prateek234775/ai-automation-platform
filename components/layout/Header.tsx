/**
 * Header – sticky top navigation bar.
 *
 * Responsibilities:
 *  - Render the site logo/wordmark
 *  - Render desktop nav links
 *  - Render CTA button(s)
 *  - Toggle mobile nav drawer
 *  - Transparent → frosted-glass on scroll
 *
 * Sub-components to build here:
 *  - NavLink      (anchor with active-state styling)
 *  - MobileMenu   (full-screen or drawer overlay)
 *  - Logo         (SVG + wordmark)
 */

export function Header() {
  return (
    <header role="banner" aria-label="Site header">
      {/* TODO: implement Header */}
      <span className="sr-only">Header placeholder</span>
    </header>
  );
}
